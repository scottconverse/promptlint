task_id: T003
workflow_id: workflow-prompt-quality-pilot
title: Workflow prompt docs and CLI coverage
role: worker
objective: Document the workflow prompt lint pilot in promptlint and add any minimal
  CLI or rule-list coverage needed to reflect the new rule without broadening the
  product.
background: Keep this slice small. Update the README and any minimal CLI coverage
  needed so the external pilot leaves behind a discoverable user-facing trace of
  the rule change.
allowed_paths:
- README.md
- tests/test_cli.py
forbidden_paths:
- promptlint/rules
required_context:
- README.md
- tests/test_cli.py
- output of T001
- git worktree C:\Users\scott\OneDrive\Desktop\Claude\promptlint-worktrees\t003
inputs:
- Rule ID and user-facing behavior chosen in T001
- Current README rule counts and category table
expected_outputs:
- Small README updates and any minimal CLI/rule-list test coverage needed to reflect the change
output_schema:
  task_id: T003
  workflow_id: workflow-prompt-quality-pilot
  status: blocked
  summary: Populate this file after the worker run completes.
  files_changed: []
  commands_run: []
  artifacts_created: []
  deviations_from_contract: []
  risks_or_concerns: []
  human_review_needed: []
  next_recommended_step: Run the worker prompt in a Codex worktree thread.
acceptance_criteria:
- The rule is discoverable in promptlint's user-facing docs or CLI coverage.
- The update does not claim a broader redesign than the pilot actually delivered.
- Changes stay limited to README and CLI-facing tests.
validation_commands:
- python -m pytest tests/test_cli.py
- python -m promptlint.cli rules --format json
constraints:
- Work only in the T003 worktree.
- Do not edit promptlint/rules in this task.
forbidden_behaviors:
- Do not broaden scope.
- Do not modify forbidden paths.
 - Do not expand into a general documentation rewrite.
estimated_complexity: medium
parallel_safe: false
depends_on:
- T001
failure_codes:
- missing_context
- validation_failed
- blocked_on_dependency
 - scope_creep
handoff_instructions: Write the result file, summarize any deviations, and hand off
  to the judge.
